<?xml version="1.0" encoding="UTF-8"?>
<relays>
    <!-- Relay Configuration -->
    <relay>
        <zoneID>1</zoneID> <!-- relay channel number -->
        <gpioPin>5</gpioPin> <!-- Corresponding GPIO Pin on the Raspberry Pi -->
        <alias>Front Yard</alias> <!-- alias --> 
        <state>LOW</state> <!-- is relay state high or low to enable) -->
    </relay>
    <!-- Add more relay entries as needed -->
</relays>